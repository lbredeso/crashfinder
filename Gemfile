source 'http://rubygems.org'

# gem 'rails', '3.1.0'
gem 'railties'
#gem 'actionpack'
gem 'actionmailer'
#gem 'activemodel'
gem 'activeresource'
gem 'tzinfo'
gem 'mongo_mapper'
gem 'bson_ext'
#gem 'couchrest', :path => "~/Code/couchrest" #:git => 'git://github.com/lbredeso/couchrest.git'
#gem 'couchrest_model', :path => "~/Code/couchrest_model"

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails', "  ~> 3.1.0"
  gem 'coffee-rails', "~> 3.1.0"
  gem 'uglifier'
end

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'

group :test do
  # Pretty printed test output
  gem 'turn', :require => false
end
